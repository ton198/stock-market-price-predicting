```
┌──────────────────────────────────────────────────────────┐
│                    ENVIRONMENT  STEP                     │
│                                                          │
│    Market prices ──► NASDAQTradingEnv.step(action)       │
│                          │                               │
│                   reward  +  next_obs                    │
└──────────────────────────────────────────────────────────┘

  Observation (6,)
  ┌─────────────────────────────────────────────────────┐
  │ LSTM prob │ 5d-ret │ vol │ pos_ratio │ cash │ regime│
  └─────────────────────────────────────────────────────┘
                              │
                    ┌─────────▼──────────┐
                    │  Linear(6 → 256)   │
                    │      + ReLU        │
                    └─────────┬──────────┘
                              │  hidden (256,)
                    ┌─────────▼──────────┐
                    │  Linear(256 → 256) │
                    │      + ReLU        │
                    └─────────┬──────────┘
                              │  hidden (256,)
                    ┌─────────▼──────────┐
                    │  Linear(256 → 31)  │   <- one Q-value per position
                    └─────────┬──────────┘
                              │
                        Q-values (31,)
                              │
               ┌──────────────▼───────────────┐
               │       e-greedy policy        │
               │  train: random w/ prob e     │
               │  eval : argmax Q             │
               └──────────────┬───────────────┘
                              │
                    action in {0 ... 30}
                    position in {-100% ... +200%}

─────────────────────────────────────────────────────────────
                    TRAINING  LOOP
─────────────────────────────────────────────────────────────

  (obs, action, reward, next_obs, done)
                    │
                    ▼
          ┌──────────────────┐
          │  Replay Buffer   │  capacity = 100 000
          │  (ring buffer)   │
          └────────┬─────────┘
                   │  sample batch (128,)
                   ▼
          ┌──────────────────────────────────────────┐
          │           TD Target (no grad)            │
          │                                          │
          │  y = r  +  g * max Q_target(s', a')      │
          │                                          │
          └──────────────┬───────────────────────────┘
                         │
                         ▼
          ┌──────────────────────────────────────────┐
          │           Huber Loss                     │
          │   L = HuberLoss ( Q(s,a), y )            │
          └──────────────┬───────────────────────────┘
                         │
                         ▼
                   Adam  (lr = 1e-4)
                         │
                         ▼
               Q-Network  weights  <──────────────────────┐
                         │                                │
                         │  hard copy every 1 000 steps   │
                         ▼                                │
               Target  Network  ──────────────────────────┘
```
